# Example environment configuration for Vite
# Duplicate this file as .env, .env.development, .env.staging, or .env.production
# depending on your deployment target. Only variables prefixed with VITE_ are exposed.

# Development (local)
VITE_API_BASE_URL=http://localhost:5222
VITE_ENABLE_STREAMING=true

# Staging (example)
# VITE_API_BASE_URL_STAGING=https://staging.example.com
# VITE_ENABLE_STREAMING=true

# Production (example)
# VITE_API_BASE_URL_PROD=https://api.example.com
# VITE_ENABLE_STREAMING=true

# Notes:
# - The app currently reads VITE_API_BASE_URL for requests.
# - For staging/prod builds, either set VITE_API_BASE_URL accordingly
#   or rely on environment-specific files like .env.production.
