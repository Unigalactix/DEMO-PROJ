# Backend environment variables (.NET reads these via configuration)
# Copy to .env and fill in for local dev; or set via system environment variables.
# Azure OpenAI settings
AzureOpenAI__Key=YOUR_AZURE_OPENAI_KEY
AzureOpenAI__Endpoint=https://YOUR_RESOURCE_NAME.openai.azure.com/
AzureOpenAI__DeploymentName=YOUR_DEPLOYMENT_NAME

# Database connection (example)
# If using a database, set a connection string here. Currently the demo uses mock services.
ConnectionStrings__Default=Server=YOUR_SQL_SERVER;Database=YOUR_DB;User Id=YOUR_USER;Password=YOUR_PASSWORD;Encrypt=True;TrustServerCertificate=True;

# Notes:
# - These map to configuration keys read in Program.cs (e.g., builder.Configuration["AzureOpenAI:Key"]).
# - ASP.NET Core supports double underscore (__) to denote nested keys.
# - If these are not set, the app uses mock services for the demo.
